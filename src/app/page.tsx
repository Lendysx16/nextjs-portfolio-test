import HeroSection from "@/components/HeroSection";
import Wrapper from "@/components/Wrapper";
const Home = async () => {
    return (
        <Wrapper>
            <HeroSection />
        </Wrapper>
    );
};

export default Home;
